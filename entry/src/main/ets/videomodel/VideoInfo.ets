export class video{
  img: ResourceStr
  play: string
  barrage: string
  title: string
  up: string
  long: string
  publish: string

  constructor(img, play, barrage, title, up, long, publish) {
    this.img = img
    this.play = play
    this.barrage = barrage
    this.title = title
    this.up = up
    this.long = long
    this.publish = publish
  }
}

